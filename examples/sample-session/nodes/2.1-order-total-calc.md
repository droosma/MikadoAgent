# Node 2.1 — OrderTotalCalc missing IPaymentService

## Identity

- **ID:** 2.1
- **Parent:** 2 (OrderService)
- **Depth:** 2
- **Status:** leaf

## Context

- **File(s):** src/Services/OrderTotalCalc.cs
- **Line(s):** 12-18
- **Worktree branch:** mikado/extract-payment-2-1

## Break Description

After updating `OrderService` to use `IPaymentService` (node 2),
`OrderTotalCalc` — a helper class used by `OrderService` — still creates a
`PaymentProcessor` instance directly to get fee calculations.

## Error Output

```text
src/Services/OrderTotalCalc.cs(15,30): error CS0246: The type or namespace name 'PaymentProcessor' could not be found
```

## Proposed Fix

Update `OrderTotalCalc` constructor to accept `IPaymentService` and use it for
fee calculations. Pass the `IPaymentService` instance from `OrderService` to
`OrderTotalCalc`.

## Children

None (leaf node)
